#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -eq 3 ] && [ "$1" = "-riqE" ]; then
  pattern="$2"
  target="$3"

  if find "$target" -print 2>/dev/null | /usr/bin/grep -iqE "$pattern"; then
    exit 0
  fi

  exec /usr/bin/grep "$@"
fi

exec /usr/bin/grep "$@"
